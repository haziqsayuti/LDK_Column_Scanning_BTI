# LDK_Column_Scanning_BTI
This tool automates the analysis of gamma column scanning count data, transforms data into interactive visualization profiles, and applies **Machine Learning (Random Forest)** and anomaly detection to automatically classify **Air / Sponge / Material** regions inside a column model.
